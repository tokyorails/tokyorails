<%= form_for(@spike) do |f| %>
  <% if @spike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spike.errors.count, "error") %> prohibited this spike from being saved:</h2>

      <ul>
      <% @spike.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
