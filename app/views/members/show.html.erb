<div class="hero-unit">

  <div class="row">
    <div class="span3">
      <%= image_tag(@member.photo.thumb('150x150#').url, :alt => @member.name) %>
    </div>
    <div class="span10">
      <h1><%= @member.name %></h1>
      <!-- twitter_username here? -->
    </div>
  </div>

  <div class="bio">
    <%= @member.bio %>
  </div>

  <h4><%= t('.interests') %></h4>
  <%= join_interests(@member) %>

  <h2><%= t('.github_projects') %></h2>
  <!-- Github projects here -->
  </div>
</div>

